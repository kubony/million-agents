# Million Agents 해커톤 개발 타임라인 (2026-01-24)

## 프로젝트 개요

**Claude Code Visual Builder** - Google Opal 스타일의 노드 기반 멀티에이전트 워크플로우 빌더

- **목표:** 비개발자도 Claude Code의 멀티에이전트 기능을 시각적으로 활용
- **기술 스택:** React + TypeScript + React Flow + Zustand + Tailwind CSS
- **실행 환경:** 로컬호스트 (DB 불필요)
- **제출 레포:** https://github.com/kubony/million-agents-plugin

---

## 스크린샷

### 워크플로우 편집 화면
![워크플로우 편집](https://github.com/kubony/million-agents-plugin/raw/main/image/2.png)

### 워크플로우 실행 화면
![워크플로우 실행](https://github.com/kubony/million-agents-plugin/raw/main/image/1.png)

---

## Phase 1: 프로젝트 초기화 (13:38 ~ 14:15)

| 시간 | Git 커밋 | 세션 활동 |
|------|----------|----------|
| 13:38 | `Initial commit: Add CLAUDE.md` | 프로젝트 구조 설정 |
| 13:43 | - | Plugin 설치 시도 (`add-project`, `cops:install-cops`) |
| 14:04 | `1st commit` | 기본 뼈대 구축 |
| 14:07 | `Add detailed project plan for Claude Code Visual Builder` | **상세 기획서(plan.md) 작성** |
| 14:15 | `참조 이미지 3장 추가` | UI 참조 이미지 추가 |

### 주요 결정사항
- React + TypeScript + React Flow 기술 스택 확정
- 5가지 노드 타입 정의: Input, Subagent, Skill, MCP, Output
- 로컬호스트 실행 (DB 불필요)

---

## Phase 2: 스킬 복사 & 초기 구현 (14:21 ~ 14:34)

| 시간 | Git 커밋 | 세션 활동 |
|------|----------|----------|
| 14:21 | `로컬 스킬 추가 및 CLAUDE.md 한글화` | COPS에서 스킬 복사/병합 |
| 14:23 | - | **스킬 배포 방법 문서화** (skill-deployment-guide.md) |
| 14:32 | `Add Visual Builder frontend and backend implementation` | **React 프론트엔드 + Express 백엔드 구현** |
| 14:34 | `Merge branch 'josh'` | 협업 브랜치 머지 |

### 구현된 기능
- 노드 캔버스 (드래그앤드롭)
- 노드 간 연결
- 우측 패널 (Preview, Console, Step)

---

## Phase 3: UI 개선 집중 (14:41 ~ 15:00)

| 시간 | Git 커밋 | 세션 활동 |
|------|----------|----------|
| 14:41 | `[UI] 핸들 연결 피드백 개선` | 노드 연결 UX 개선 |
| 14:46 | `[UI] 노드 박스 디자인 개선 - memorialbanana 스타일 적용` | **memorialbanana 프로젝트 UI 참고** |
| 14:49 | `[스킬] git-commit-push에 PR 생성 및 머지 기능 추가` | 개발 도구 스킬 강화 |
| 14:50 | `dist 폴더를 git 트래킹에서 제거` | Git 정리 |
| 14:57 | `저장/내보내기 기능 구현` | **localStorage + JSON 내보내기** |
| 14:59 | `[UI] Visual Builder 대폭 개선` | **메뉴 구조 개편** (Subagent/Skill/MCP 분리) |

### 핵심 개선
- memorialbanana 프로젝트에서 UI 영감 받음
- 노드 연결 애니메이션 개선
- 저장/불러오기 기능 추가

---

## Phase 4: AI 워크플로우 생성 (15:00 ~ 15:20)

| 시간 | Git 커밋 | 세션 활동 |
|------|----------|----------|
| 15:00 | - | **"워크플로우 자동 생성" 기획** |
| 15:12 | `[UI] 노드 스타일 개선 - 흰색 배경 제거 및 연결 포인트 확대` | UI 마무리 |
| 15:13 | PR #1 머지 | feature/visual-builder-ui-fix 완료 |
| 15:18 | `[기능] 워크플로우 AI 생성 및 터미널 서비스 추가` | **Gemini API 연동** |

### 새로운 기능
- "Describe your workflow" 입력 → AI가 노드 자동 생성
- `.env`에 GEMINI_API_KEY 저장
- 터미널 서비스 추가 (`claude -c` 연동)

---

## Phase 5: 실행 엔진 구현 (15:20 ~ 16:00)

| 시간 | Git 커밋 | 세션 활동 |
|------|----------|----------|
| 15:45 | `[기능] 워크플로우 실행 서비스 구현 및 UI 개선` | **RUN 버튼으로 실제 실행** |
| 15:57 | `[기능] 트리 레이아웃 알고리즘 추가 및 워크플로우 실행 개선` | 노드 자동 정렬 알고리즘 |
| 16:02 | `[수정] PptxGenJS 생성자 호출 오류 수정 및 UI 개선` | 버그 수정 |

### 실행 흐름
```
Input → 토폴로지 정렬 → 병렬 실행 → Output
```

---

## Phase 6: 문서화 & 마무리 (16:19 ~ 16:24)

| 시간 | Git 커밋 | 세션 활동 |
|------|----------|----------|
| 16:19 | `Add screenshot images for documentation` | 스크린샷 추가 |
| 16:20 | `Add README with project documentation and screenshots` | README 작성 |
| 16:24 | `Add visual-builder skill and update README for Claude Code plugin` | **플러그인 형태로 패키징** |

### 최종 산출물
- Visual Builder 웹앱
- Claude Code 플러그인 스킬

---

## Phase 7: 플러그인 제출 (16:01 ~ 16:24) - million-agents-plugin 레포

플러그인 형태로 별도 레포지토리에 제출

| 시간 | Git 커밋 | 설명 |
|------|----------|------|
| 16:01 | `1st commit` | 플러그인 초기 구조 생성 |
| 16:05 | `문서 업데이트` | PLUGIN.md 작성 |
| 16:08 | `소개 문서 추가` | docs 폴더 추가 |
| 16:24 | `이미지 추가` | 스크린샷 2장 추가 |

### 플러그인 구조
```
million-agents-plugin/
├── .claude/
│   ├── skills/workflow-builder/SKILL.md   # 스킬 정의
│   └── commands/workflow-builder.md       # 커맨드 정의
├── src/                                    # 프론트엔드 소스
├── server/                                 # 백엔드 소스
├── scripts/
│   ├── install.sh                          # 설치 스크립트
│   └── register.sh                         # 플러그인 등록
├── image/
│   ├── 1.png                               # 실행 화면
│   └── 2.png                               # 편집 화면
├── README.md                               # 사용 가이드
└── PLUGIN.md                               # 플러그인 메타데이터
```

---

## 노드 타입 상세

| 노드 | 아이콘 | 설명 | 설정 항목 |
|------|--------|------|----------|
| **Input** | 📥 | 사용자 입력 | type: text/file/select |
| **Subagent** | 🤖 | AI 에이전트 | role, tools, model, systemPrompt |
| **Skill** | ⚡ | 스킬 실행 | skillId, args |
| **MCP** | 🔌 | 외부 연동 | type: stdio/sse/http, serverConfig |
| **Output** | 📤 | 결과 출력 | format: markdown/image/document |

### 연결 규칙
- Input → Subagent/Skill/MCP 가능
- Subagent → Subagent/Skill/Output 가능
- Skill → Skill/Output 가능
- MCP → Subagent/Output 가능
- Output은 종착점 (연결 대상 없음)
- 순환 연결 금지 (A → B → A)

---

## 지원 스킬 목록

| 스킬 | 설명 |
|------|------|
| `image-gen-nanobanana` | Google Gemini 이미지 생성 |
| `video-gen-veo3` | Google Veo3 영상 생성 |
| `ppt-generator` | 미니멀 프레젠테이션 생성 |
| `pdf` | PDF 처리 (추출, 생성, 병합) |
| `docx` | Word 문서 편집 |
| `xlsx` | Excel 스프레드시트 |
| `pptx` | PowerPoint 프레젠테이션 |
| `git-commit-push` | Git 커밋/푸시 자동화 |

---

## 최종 프로젝트 구조 (개발 레포)

```
million-agents/
├── src/
│   ├── components/
│   │   ├── canvas/FlowCanvas.tsx      # 노드 캔버스
│   │   ├── nodes/                     # 5가지 노드 타입
│   │   │   ├── InputNode.tsx
│   │   │   ├── SubagentNode.tsx
│   │   │   ├── SkillNode.tsx
│   │   │   ├── McpNode.tsx
│   │   │   └── OutputNode.tsx
│   │   ├── panels/                    # 우측 패널
│   │   │   ├── PreviewPanel.tsx
│   │   │   ├── ConsolePanel.tsx
│   │   │   └── StepPanel.tsx
│   │   └── dialogs/SaveDialog.tsx
│   ├── services/
│   │   ├── aiService.ts               # Gemini AI 연동
│   │   ├── workflowGenerator.ts       # AI 워크플로우 생성
│   │   ├── localExecutor.ts           # 로컬 실행
│   │   └── socketService.ts           # 터미널 연동
│   ├── stores/
│   │   ├── workflowStore.ts           # 워크플로우 상태
│   │   ├── executionStore.ts          # 실행 상태
│   │   └── panelStore.ts              # 패널 상태
│   ├── hooks/
│   │   └── useWorkflowExecution.ts    # 실행 훅
│   ├── types/
│   │   ├── workflow.ts
│   │   ├── nodes.ts
│   │   └── save.ts
│   └── utils/
│       ├── connectionValidator.ts     # 연결 검증
│       ├── markdownGenerator.ts       # MD 생성
│       └── claudeConfigGenerator.ts   # 설정 파일 생성
├── server/
│   ├── services/
│   │   ├── claudeService.ts           # Claude API 연동
│   │   ├── skillExecutionService.ts   # 스킬 실행
│   │   ├── workflowExecutionService.ts # 워크플로우 실행
│   │   └── fileService.ts             # 파일 저장
│   └── index.ts                       # Express 서버
├── .claude/skills/                    # Claude Code 스킬
├── docs/
│   ├── hackathon-development-timeline.md  # 이 문서
│   └── skill-deployment-guide.md      # 스킬 배포 가이드
└── plan.md                            # 상세 기획서
```

---

## 개발 통계

| 항목 | 수치 |
|------|------|
| 총 커밋 수 (개발 레포) | 24개 |
| 총 커밋 수 (플러그인 레포) | 4개 |
| 총 세션 수 | 약 20개 |
| 개발 시간 | 약 3시간 (13:38 ~ 16:24) |
| 주요 기여자 | 팀 협업 (josh 브랜치 머지 포함) |

---

## 구현 완료된 MVP 기능

- [x] 노드 캔버스 (드래그앤드롭, 연결)
- [x] 5가지 노드 타입 (Input, Subagent, Skill, MCP, Output)
- [x] AI 워크플로우 자동 생성 (Gemini API)
- [x] 워크플로우 실행 (터미널 연동)
- [x] 저장/불러오기 (localStorage + JSON)
- [x] 트리 레이아웃 자동 정렬
- [x] Claude Code 설정 파일 내보내기 (.claude/ 구조)
- [x] 플러그인 패키징 및 GitHub 제출

---

## 사용법

### 빠른 시작

```bash
# 1. 레포 클론
git clone https://github.com/kubony/million-agents-plugin.git
cd million-agents-plugin

# 2. 의존성 설치
npm install

# 3. 환경 설정
cp .env.example .env
# .env 파일에 ANTHROPIC_API_KEY, GEMINI_API_KEY 설정

# 4. 실행
npm run dev:all

# 5. 브라우저에서 http://localhost:5173 접속
```

### 워크플로우 만들기

1. **노드 추가**: 상단 팔레트에서 노드를 캔버스로 드래그
2. **노드 연결**: 노드의 핸들을 드래그하여 다른 노드에 연결
3. **설정 편집**: 노드 클릭 후 우측 Step 패널에서 상세 설정
4. **실행**: Preview 패널에서 입력값 설정 후 실행 버튼 클릭
5. **저장**: Header의 Save 버튼으로 Claude Code 설정 파일 내보내기

### AI로 워크플로우 생성

```
"주제를 리서치하고, 블로그 글을 작성한 후, 썸네일 이미지를 생성해줘"
```
→ Input → Researcher → Writer → Image Generator → Output 자동 생성

---

## 워크플로우 예시

### 블로그 글 작성 파이프라인
```
┌─────────┐    ┌────────────┐    ┌──────────┐    ┌────────────┐
│  Input  │───▶│ Researcher │───▶│  Writer  │───▶│   Output   │
│  (주제) │    │  (리서치)  │    │  (작성)  │    │ (마크다운) │
└─────────┘    └────────────┘    └──────────┘    └────────────┘
```

### 프레젠테이션 생성
```
┌─────────┐    ┌──────────┐    ┌─────────────┐    ┌────────┐
│  Input  │───▶│ Analyzer │───▶│     PPT     │───▶│ Output │
│ (데이터)│    │  (분석)  │    │ (슬라이드) │    │ (파일) │
└─────────┘    └──────────┘    └─────────────┘    └────────┘
```

### 복합 콘텐츠 생성
```
                              ┌──────────────┐    ┌────────────┐
                         ┌───▶│ Image Gen    │───▶│   Output   │
┌─────────┐    ┌────────┐│    │ (썸네일)     │    │  (이미지)  │
│  Input  │───▶│ Writer ││    └──────────────┘    └────────────┘
│  (주제) │    │ (글)   ││
└─────────┘    └────────┘│    ┌──────────────┐    ┌────────────┐
                         └───▶│   Output     │───▶│   Output   │
                              │  (마크다운)  │    │   (문서)   │
                              └──────────────┘    └────────────┘
```

---

## 향후 계획 (추후 구현)

- [ ] 실제 Claude Code CLI 연동 강화
- [ ] 워크플로우 템플릿 갤러리
- [ ] 협업 기능 (실시간 공동 편집)
- [ ] 버전 관리 및 히스토리
- [ ] 조건부 분기 노드
- [ ] 반복 (Loop) 노드
- [ ] 외부 API 연동 노드

---

## 링크

- **제출 레포:** https://github.com/kubony/million-agents-plugin
- **개발 레포:** /Users/inkeun/projects/million-agents
- **플러그인 레포:** /Users/inkeun/projects/million-agents-plugin

---

*이 문서는 2026-01-24 해커톤 세션 히스토리와 Git 히스토리를 기반으로 자동 생성되었습니다.*
*최종 업데이트: 2026-01-25*
