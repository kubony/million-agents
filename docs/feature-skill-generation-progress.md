# 스킬 생성 진행 상황 표시 기능

**브랜치:** `feature/skill-generation-progress`
**날짜:** 2026-01-25

## 개요

비개발자가 스킬 생성 과정을 쉽게 이해할 수 있도록 실시간 진행 상황을 표시하는 기능을 구현했습니다.

## 구현된 기능

### 1. 스킬 생성 진행 상황 실시간 표시

Socket.IO를 통해 스킬 생성 과정을 단계별로 Console 패널에 표시합니다.

| 단계 | 메시지 |
|------|--------|
| started | ✨ 스킬 생성을 시작합니다 |
| analyzing | 🔍 요청을 분석하고 있어요 |
| designing | 📝 스킬 구조를 설계하고 있어요 |
| generating | ⚙️ 코드를 생성하고 있어요 |
| saving | 💾 파일을 저장하고 있어요 |
| installing | 📦 패키지를 설치하고 있어요 |
| completed | ✅ 스킬이 생성되었습니다! |

**구현 파일:**
- `server/services/skillGeneratorService.ts` - 진행 상황 콜백 추가
- `server/index.ts` - Socket.IO 이벤트 `generate:skill` 추가
- `src/services/socketService.ts` - 클라이언트 이벤트 타입 정의
- `src/components/layout/PromptBar.tsx` - Socket.IO 기반 스킬 생성

### 2. 스킬 미리보기

생성된 스킬 노드를 클릭하면 Step 패널에서 코드를 미리볼 수 있습니다.

- SKILL.md, main.py, requirements.txt 파일 내용 표시
- 아코디언 스타일 UI로 파일별 펼치기/접기
- 스킬 경로 표시

**구현 파일:**
- `server/index.ts` - `/api/skill/files` API 추가
- `src/components/panels/StepPanel.tsx` - SkillSettings 컴포넌트 수정

### 3. 의존성 자동 설치

스킬 생성 완료 후 requirements.txt가 있으면 자동으로 패키지를 설치합니다.

- **uv 우선 사용** (10-100x 빠름)
- uv가 없으면 pip으로 fallback
- 전역 venv (`~/.claude/venv/`) 사용

**설치 우선순위:**
1. `uv pip install --python ~/.claude/venv/bin/python -r requirements.txt`
2. `~/.claude/venv/bin/pip install -r requirements.txt`
3. `pip3 install -r requirements.txt`

**구현 파일:**
- `server/services/skillGeneratorService.ts` - `installDependencies()` 메서드

### 4. 스킬 테스트 실행

Step 패널에서 생성된 스킬을 바로 테스트할 수 있습니다.

- 테스트 실행 버튼
- 인자 입력 지원 (space-separated)
- 실행 결과 (stdout/stderr) 표시
- 30초 타임아웃

**구현 파일:**
- `server/index.ts` - `/api/skill/test` API 추가
- `src/components/panels/StepPanel.tsx` - 테스트 UI 추가

### 5. 스킬 노드 자동 추가

스킬 생성 완료 시 캔버스에 노드가 자동으로 추가되고 선택됩니다.

- 새 노드 위치 자동 계산
- Step 패널 자동 열기

**구현 파일:**
- `src/types/nodes.ts` - `SkillNodeData`에 `skillPath` 필드 추가
- `src/components/layout/PromptBar.tsx` - `handleSkillCompleted`에서 노드 추가

## 커밋 히스토리

```
f1559cb feat: 스킬 생성 진행 상황 실시간 표시 기능 추가
e0ad45a feat: 스킬 미리보기, 의존성 설치, 테스트 실행 기능 추가
07dfd0d perf: 의존성 설치 시 uv 우선 사용
```

## 테스트 방법

```bash
cd /Users/inkeun/projects/million-agents-feature-skill-generation-progress
npm run dev:all
```

브라우저에서 `http://localhost:3003` 접속 후:

1. "gmail 스킬 만들어줘" 입력
2. Console 패널에서 진행 상황 확인
3. 생성된 노드 클릭 → Step 패널에서 코드 미리보기
4. "테스트 실행" 버튼으로 스킬 실행

## 기술 스택

- **실시간 통신:** Socket.IO
- **패키지 설치:** uv (우선) / pip (fallback)
- **UI 상태 관리:** Zustand (executionStore, panelStore)

## 향후 개선 사항

- [ ] 스킬 수정 기능 (코드 에디터)
- [ ] 스킬 갤러리 (템플릿 선택)
- [ ] 스킬 체이닝 (여러 스킬 연결)
- [ ] 스킬 공유 기능
